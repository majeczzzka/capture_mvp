import 'package:flutter/material.dart';

/// A utility class that defines color constants used throughout the app.
class AppColors {
  static const background =
      Color.fromRGBO(239, 238, 231, 1); // Main background color
  static const jarGridBackground =
      Color.fromRGBO(255, 255, 255, 1); // Background for jar grid area
  static const fonts = Color.fromRGBO(168, 164, 139, 1); // Primary font color
  static const navBar = Color.fromRGBO(
      221, 219, 205, 1); // Background color for the navigation bar
  static const selectedFonts =
      Color.fromRGBO(138, 135, 117, 1); // Color for selected font items
}
